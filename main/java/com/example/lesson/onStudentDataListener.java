package com.example.lesson;

import java.util.ArrayList;

import models.Group;
import models.Student;

public interface onStudentDataListener {
    void onStudentListener(ArrayList<Student> students);
}
